from Sudoku9x9 import Sudoku9x9

def Sudoku9x9Solver():
    def __init__(self, square = None):
        self.ssquare = Sudoku9x9(square)
        # Basically just that, really

    # Helper Functions
    # checkEmpty: check if there are empty squares
    def checkEmpty(self):

        ssquare.printSquare()

        isEmpty = False
        for row in range(0, len(self.ssquare)):
            if 0 in self.ssquare[row]:
                isEmpty = True
    # checkBlocked: check if there are any squares blocked

    